# Backend dependencies for the YouTube Key Takeaways API.
#
# Only the packages the running app actually imports are listed here. pip will
# resolve their transitive dependencies automatically, so this file stays small
# and the Vercel/serverless build stays fast.
#
# NOTE: backend/sections.py is a standalone experimental script that relies on a
# heavy ML stack (spaCy, torch, transformers, nltk). It is NOT used by the web
# app and is intentionally excluded here. Install those separately if you want
# to run it.

# Web framework
Flask==3.0.3
Flask-Cors==4.0.1

# Claude — official Anthropic SDK. Powers the two-pass extractor/verifier
# pipeline in transcript_processor.py (structured outputs + adaptive thinking).
anthropic==0.111.0

# Structured-output schema validation used by transcript_processor.py
pydantic==2.9.2

# Transcript fetching (1.x API: YouTubeTranscriptApi().fetch(...).to_raw_data())
youtube-transcript-api==1.2.3

# Loads ANTHROPIC_API_KEY / ALLOWED_ORIGINS from a .env file (optional at runtime)
python-dotenv==1.0.1
